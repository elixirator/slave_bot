defmodule Slave.Responders.NotesTest do
  use Hedwig.RobotCase
  # TODO: Fix it
  # @tag start_robot: true, name: "slave", responders: [{Slave.Responders.Notes, []}]
  # test "great success - responds with a borat url", %{adapter: adapter, msg: msg} do
  #   send adapter, {:message, %{msg | text: "note hello world"}}
  #   assert_receive {:message, %{text: text}}
  #   assert String.contains?(text, "Thanks.")
  # end
end
